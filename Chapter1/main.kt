// 컴파일 및 실행 방법:
// 컴파일: kotlinc main.kt -include-runtime -d main.jar
//     (이때 확장자 .kt와 .jar를 반드시 포함시켜야 함)
// 실행: java -jar main.jar
fun main(argv: Array<String>) {
    println("Hello, World!");
}